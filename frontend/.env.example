# Local dev (browser calls FastAPI directly)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel production (server-side proxy — set in Vercel dashboard, redeploy required)
BACKEND_URL=https://workout-form-coach-api.onrender.com
