services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8000:8000"
  demo_app:
    build:
      context: demo_app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
